{
  "name": "log-platform",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "frontend",
    "backend",
    "worker",
    "packages/*"
  ],
  "scripts": {
    "dev:frontend": "npm run dev -w frontend",
    "dev:backend": "npm run dev -w backend",
    "dev:worker": "npm run dev -w worker",
    "test": "tsx --test tests/backend/tenant-scope.test.ts",
    "test:integration": "tsx --test tests/worker/evaluator.test.ts"
  }
}
